apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ce-server-data
  namespace: ce-server
spec:
  storageClassName: proxmox
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi